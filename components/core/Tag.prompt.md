Pill chip for tech-stack / skill labels (e.g. inside project cards or the "Sobre mí" tag cloud). Hovers to the morado accent.

```jsx
<Tag>React</Tag>
<Tag size="sm">Node.js</Tag>
```

Sizes: `sm | md`.
