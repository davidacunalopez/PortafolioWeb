Hero stat — a big Space Grotesk number over a muted label. Group several in a glass strip with `StatDivider` between.

```jsx
<div style={{display:'flex',gap:'1.5rem',alignItems:'center'}}>
  <StatBlock value="+10" label="Proyectos" />
  <StatDivider />
  <StatBlock value="3+" label="Años exp." />
</div>
```
