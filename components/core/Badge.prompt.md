Soft tinted status pill. Use `dot` for the hero's "available to work" indicator (pulsing dot), or as a plain category badge.

```jsx
<Badge tone="info" dot>Disponible para trabajar</Badge>
<Badge tone="accent">Frontend</Badge>
```

Tones: `accent` (morado) · `info` (cyan) · `pink` (rosa).
