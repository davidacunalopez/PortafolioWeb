Labelled progress bar with a gradient fill that animates to `value`% on mount. Used in the "Frontend" bento tile.

```jsx
<SkillBar label="HTML / CSS" value={95} />
<SkillBar label="JavaScript" value={85} />
```
